# OpenAI image API key (required only for the "From text" / AI generation mode).
# Image -> pixel conversion works without this.
OPENAI_API_KEY=

# Optional: model to use for image generation (default: gpt-image-1)
OPENAI_IMAGE_MODEL=gpt-image-1
